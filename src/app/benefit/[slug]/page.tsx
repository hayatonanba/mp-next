interface BenefitProps {
  params: {
    slug: string;
  }
}

export default function Benefit({ params }: BenefitProps) {

    return (
        <div>
         <h2>mock page</h2>
        </div>
    );
}
